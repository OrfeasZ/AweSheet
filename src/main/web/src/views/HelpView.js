import React, { Component, PropTypes } from 'react'

export default class HelpView extends Component
{
    render()
    {
        return (
            <div>
            </div>
        );
    }
}
